const calc = (a: number, b: number) => {
    return a - b;
};
console.log(calc(1024, 28));
export default calc;
// git提交规范(提供交互界面) npx commitizen init cz-conventional-changelog --save-dev --save-exact
